# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# LangGraph + LangChain (agent orchestration and tool use)
langgraph>=0.2.28
langchain>=0.3.1
langchain-openai>=0.2.1
langchain-core>=0.3.6

# OpenAI SDK (LLM calls)
openai>=1.51.0

# Data processing (CSV analysis, scoring model)
pandas>=2.2.0
numpy>=1.26.0

# Pydantic v2 (structured output validation on all models)
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Retry logic with exponential backoff
tenacity>=8.2.0

# Structured logging
structlog>=24.0.0

# PDF generation (10-page acquirer report)
reportlab>=4.2.0

# HTTP client (for inter-service calls if needed)
httpx>=0.27.0

# CORS + SSE support
sse-starlette>=2.1.0

# Environment variable management
python-dotenv>=1.0.0
